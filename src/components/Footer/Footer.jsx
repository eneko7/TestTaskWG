import React from 'react';
import styles from './Footer.scss';

const Footer = () => (
  <footer className={styles.footer}>
    <span>Front-end test task for wargaming company</span>
  </footer>
);

export default Footer;
